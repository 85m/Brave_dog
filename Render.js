function render() {

    // Camera
    //game.debug.cameraInfo(game.camera, 32, 32);


    //game.debug.spriteBounds(playerRing);
    //game.debug.spriteBounds(scent);
    //game.debug.spriteBounds(wall);

    game.debug.geom(playerRing[0].circleData,'rgba(255,0,0,.2)');
    //game.debug.geom(itemRing[0].circleData,'#ff0000');
}